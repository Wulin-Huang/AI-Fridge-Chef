# AI 冰箱菜谱助手 · 简历与面试素材

> 配套项目代码见本目录。以下内容可直接用于简历、项目介绍与面试准备。

---

## 一、简历项目经历（可直接粘贴）

**AI 冰箱菜谱助手 —— 个人全栈项目**（2026.09）

- 独立开发基于 DeepSeek 大模型的全栈智能菜谱应用（React 18 + TypeScript / FastAPI + SQLAlchemy），实现食材管理、**拍照识别食材**、保质期预警、AI 菜谱生成、多日菜单规划、购物清单、营养分析、**做菜打卡与成就系统**、**AI 美食评审**九大模块的完整闭环
- 接入 DeepSeek 视觉模型（v4-flash-vision-exp）实现两个多模态功能：拍照识别食材（Canvas 端侧压缩，数 MB 原图压至数百 KB，实测 5 种食材 5/5 命中、~3s 返回）与成品菜 AI 点评（色泽/摆盘打分 + 改进建议），识别结果带置信度供用户勾选修正
- 设计"零固定数据"生成架构：不预置任何菜谱库/枚举数据，通过随机灵感因子注入 + 动态温度扰动（0.75~1.15）+ 历史排除列表三层机制，保证相同输入每次生成不同结果，彻底解决 LLM 输出固化问题
- 基于 SSE（Server-Sent Events）实现流式菜谱生成：后端逐块转发 DeepSeek 流式响应，前端自研**部分 JSON 渐进解析器**，在 JSON 未闭合时实时提取已完成字段，实现"菜名先现、描述逐字、步骤逐条"的打字机渲染，首屏反馈时间从 8s+ 缩短至 2s
- 设计"保质期拯救链路"：食材卡片化管理（emoji 图标库 + 过期角标 + 编辑弹层）→ 常见食材入库自动补默认冷藏保质期（精确匹配表 + 多字关键词优先兜底，两级查找）→ 快过期食材橙色预警专区（支持已过期一键清理）→ 一键跳转菜谱页自动以"优先消耗快过期食材"方向流式生成，形成防浪费的业务闭环
- 丰富冰箱管理体验：关键词搜索 + 八大分类横向筛选（本地分类映射规则）、常买食材记忆（localStorage 记录历史添加、一键补回）、AI 冰箱体检（LLM 分析食材营养结构，输出 A/B/C/D 评级 + 亮点/缺口/采购建议）；食材卡片升级为 AI 生成的 3D 黏土风形象图（静态资源 + 名称精确/包含匹配、emoji 兜底的降级策略）
- 打磨生成体验的产品细节：菜谱页支持食材参与筛选（精确控制生成方向）、快捷偏好标签、生成道数（3~5 道）与往期批次归档；菜单规划支持餐次自定义（早/午/晚）、缺料自动汇总为可勾选采购清单（localStorage 持久化）、菜单保存回看与一键复制导出
- 封装 LLM 结构化输出链路：`json_object` 响应模式 + 三重容错解析 + 自动重试；为打卡/成就/菜单保存/保质期映射等模块编写 47 个测试用例（TestClient + 内存数据库隔离），连续打卡天数等纯函数边界全覆盖

**技术栈**：React 18 / TypeScript / TailwindCSS / Zustand / Vite / FastAPI / SQLAlchemy 2.0 / SQLite / DeepSeek API（Chat + Vision）/ SSE

---

## 二、30 秒口头介绍（面试开场）

"我做一个 AI 全栈项目叫冰箱菜谱助手，从食材到餐桌的完整闭环：拍照识别食材、AI 生成菜谱、规划一周菜单，做完菜还能打卡点亮成就、拍成品照让 AI 美食评审打分。技术上有四个我比较得意的设计：一是两个多模态场景——食材识别和成品菜点评，都接的 DeepSeek 视觉模型，前端做端侧压缩控成本；二是'零固定数据'——项目里没有任何预置菜谱，全部由大模型实时生成，我用随机灵感因子加动态温度扰动解决了输出千篇一律的问题；三是流式体验——菜谱生成走 SSE 逐块推送，我自研了部分 JSON 渐进解析器，JSON 没收完就能边解析边渲染；四是保质期拯救链路——快过期食材自动预警，一键让 AI 优先围绕它们出菜谱，是个有产品思维的防浪费设计。"

---

## 三、核心技术点详解（面试展开用）

### 1. 多模态拍照识别（视觉模型）

- **模型**：`deepseek-v4-flash-vision-exp`（2026.08 上线的实验性视觉模型，图片按尺寸折算 token 计费），OpenAI 兼容的 `image_url` + base64 data URL 格式
- **成本控制**：手机原图动辄 3~5MB，前端 Canvas 端侧压缩（最长边 1024px / JPEG 质量 0.82）→ 数百 KB 再上传；后端限制 base64 上限 6M 字符兜底
- **交互设计**：识别结果带置信度（0~1），前端默认勾选 ≥0.6 项，用户可增删后一键入库——"AI 提议、人确认"，避免误识别直接污染数据
- **Prompt 细节**：要求忽略餐具/背景、不确定不猜测、已有食材不重复列出；视觉模型带思维链（reasoning_content），max_tokens 必须给足否则思考耗尽 token 后 content 为空（实测踩坑）
- **实测数据**：5 种食材样图 5/5 命中（置信度 90%~98%），耗时 ~3s

### 2. 防 LLM 输出固化（三层机制）

| 层 | 机制 | 位置 |
|---|---|---|
| 数据层 | 不预置菜谱/菜系/词库，全部由模型生成 | 后端无任何种子数据 |
| Prompt 层 | 每次请求随机注入"灵感因子"（时令搭配/快手菜/异国风味/减脂需求等 10 种方向） | `prompts.py _spark()` |
| 采样层 | temperature 在 0.75~1.15 随机扰动；生成类高温（多样性）、营养分析低温（准确性） | `deepseek.py creative_temperature()` |
| 业务层 | `excluded` 携带历史生成菜名，"再来一批"不重样 | 前后端联动 |

### 3. SSE 流式 + 部分 JSON 渐进解析

- 后端：`chat.completions.create(stream=True)` 逐块转发为 `data: {...}\n\n` 事件，`[DONE]` 终止；`X-Accel-Buffering: no` 防代理缓冲
- 前端：`fetch` + `ReadableStream.getReader()` 手写 SSE 解析（非 EventSource，因为需要 POST）
- **渐进解析器**（`partialJson.ts`）核心算法：
  - 字符级扫描器跟踪字符串/转义/嵌套深度，在未闭合 JSON 中切出"完整对象 + 未闭合尾部"
  - 已完成字段正则提取；正在输入的字段带 `▌` 光标实时渲染
  - 踩过的坑（可讲）：全局正则 `exec` 失败后 `lastIndex` 重置为 0，导致尾部检测错位——改为循环内记录最后成功位置
- 渲染节流：80ms 合并一次 setState，避免高频重渲染

### 4. LLM 结构化输出可靠性

- `response_format={"type": "json_object"}` 强约束输出为 JSON
- 解析三重兜底：直接 `json.loads` → 剥离 ```json 代码块 → 花括号截取
- 解析失败自动重试 1 次；错误统一包装为 503 语义返回前端

### 5. 全栈工程

- 后端分层：routers（路由）/ schemas（Pydantic 校验）/ models（ORM）/ deepseek（LLM 客户端）/ prompts（模板集中管理）
- SQLAlchemy 2.0 `Mapped[]` 注解风格；前端 Zustand 全局状态 + API 层统一错误处理
- Vite 开发代理解决跨域；`.env` + python-dotenv 管理密钥

---

## 四、面试预演 Q&A

**Q: 拍照识别为什么在前端压缩而不是后端？**
A: 成本和体验双重考虑：手机原图 3~5MB，直接上传慢且费流量；视觉模型按图片尺寸折算 token 计费，大图直接放大成本。前端 Canvas 压到 1024px/数百 KB，识别精度实测不受影响（食材识别不需要 4K 细节），上传快、计费省。后端再做 6M 字符上限兜底防绕过。

**Q: 视觉识别有误识别怎么办？**
A: 我采用"AI 提议、人确认"的交互模式：模型返回置信度，前端默认只勾选 ≥0.6 的项，用户可以逐项增删再入库。识别准确性要求高的场景（健康数据、支付）也可以复用这套置信度门槛设计。

**Q: 为什么用 SSE 而不是 WebSocket？**
A: 场景是"服务端单向推送生成内容"，SSE 更轻量、天然支持自动重连、走普通 HTTP 无需升级协议；WebSocket 的双向能力在这里用不上。且需要 POST 携带大量上下文，我直接用 fetch 读 ReadableStream 手写 SSE 解析，比 EventSource（只支持 GET）更合适。

**Q: 部分 JSON 解析为什么不用现成库？**
A: 评估过 partial-json 类库，但我的场景是"提取特定字段 + 数组元素渐进渲染 + 识别正在输入的字段"，定制逻辑不复杂（核心 ~150 行），自研可控性更好，也顺手处理了转义、嵌套、数组内字符串等边界。

**Q: 怎么保证 AI 输出质量/格式稳定？**
A: 三方面：Prompt 里明确 JSON Schema 并给字段示例；API 层用 json_object 模式硬约束；解析层多重兜底 + 重试。温度按场景分化——创意生成高温，营养分析等事实型输出低温。

**Q: 项目最难的部分？**
A: 流式渐进解析的边界情况：比如字符串内的转义引号会破坏简单的括号计数，必须做字符级状态机（inString/escape/depth 三状态）；全局正则 lastIndex 重置的坑也是实测单测发现的——我给解析器写了 9 个流式中间态断言用例。

**Q: 如果要上线生产，你会做什么？**
A: SQLite → PostgreSQL；食材建议类低频变化内容加 Redis 缓存降成本；SSE 网关加超时与断线重连；视觉识别加多图拼拍与识别结果缓存；加用户系统与配额控制。

---

## 五、可量化表述（面试引用）

- 视觉识别：5 种食材样图 5/5 命中（置信度 90%~98%），端到端 ~3s；图片压缩后体积降至原图约 1/10
- SSE 流式：首块响应 ~2s（对比非流式整体等待 8~10s），数据块 1600+/次
- 渐进解析器：9 个流式中间态单测全通过，覆盖转义/嵌套/未闭合数组等边界
- 打卡/成就/统计：20 个测试用例全通过（TestClient + 内存数据库隔离），含连续天数等纯函数边界
- 菜单保存 CRUD：12 个测试用例全通过（Schema 校验 + 增删查闭环 + 404 边界）
- 保质期映射/默认填充：15 个测试用例全通过（精确/关键词匹配、自动填日期、未知食材不填、空冰箱体检校验）
- 零预置数据：后端无任何菜谱种子数据，9 个业务模块中 7 个由 LLM 实时驱动（打卡/成就走规则计算）
- 前端构建：React+TS 生产包 gzip ~79KB（5 页面 + 10 组件）
